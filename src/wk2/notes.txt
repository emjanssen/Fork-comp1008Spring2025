Task 1
Modify budget (budget2)
Ask the user for a limit
    Make sure limit is between 200 and 1000
        if not, end the program: "Sorry, but you entered invalid values"

Only allow user to enter and expense value that is 15% or less than their budget
    if not, break out of loop and output sorry message

Bonus: allow user enter a yearly value and you transform it to a
        monthly value



Notes

Two Classes
    Expense
        Name
        Amount
    Budget
        Series/Collection of Expenses ^
        minLimit, maxLimit, limit
        maxOneTimePurchase

getters and setters
    used only for private instance variables
    gives access to an otherwise inaccessible instance variable

